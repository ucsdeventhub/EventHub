# Use case compiler

## Requirements

* `go` compiler
* `pandoc` - this can probably be removed with a go library
* `wkhtmltopdf`

## Running

Set the following environment variables:

* `GITHUB_TOKEN`
